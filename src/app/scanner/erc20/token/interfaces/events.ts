export enum Erc20EventSignature {
  Approval = "Approval(address,address,uint256)",
  Transfer = "Transfer(address,address,uint256)",
}

export enum Erc20EventTypes {
  Approval = "Approval",
  Transfer = "Transfer",
}
